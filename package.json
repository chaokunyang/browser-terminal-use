{
  "name": "browser-terminal-use",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension + local bridge + CLI for browser terminal automation",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @browser-terminal-use/core && npm run build --workspace @browser-terminal-use/bridge && npm run build --workspace @browser-terminal-use/cli",
    "test": "npm run test --workspace @browser-terminal-use/core && npm run test --workspace @browser-terminal-use/bridge && npm run test --workspace @browser-terminal-use/cli",
    "lint": "npm run lint --workspace @browser-terminal-use/core && npm run lint --workspace @browser-terminal-use/bridge && npm run lint --workspace @browser-terminal-use/cli",
    "dev:bridge": "npm run dev --workspace @browser-terminal-use/bridge",
    "dev:cli": "npm run dev --workspace @browser-terminal-use/cli",
    "start:browterm-daemon": "node packages/bridge/dist/index.js",
    "start:browterm": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
